# NeNe 商用利用可否 評価レポート

評価日: 2026-05-23

---

## 結論

**用途と規模を選べば十分耐えられる。**

---

## 商用利用できる領域

| 用途 | 評価 |
|---|---|
| 社内業務ツール（受発注・在庫・勤怠 等） | 適している |
| レガシー PHP システムのリプレース | まさに設計目標 |
| 認証付き REST API（モバイルアプリ等のバックエンド） | 問題なし |
| 数十〜数百ユーザーの小規模 SaaS | 追加実装が必要 |
| 大量ファイルを扱うシステム | 向いていない |
| 高トラフィック外部向け Web サービス | 向いていない |

---

## 商用利用で気になる具体的な問題点

### 致命的ではないが要対処

**1. マイグレーション機構がない（最大の実務懸念）**

スキーマ変更は `ALTER TABLE` を手動実行。ADR-0005 でも「マイグレーションは対象外」と明記されています。本番運用中にカラム追加・変更があるたびに、オペレータが手で SQL を叩く必要があります。

**2. ファイル送信は全量メモリ展開**

`sendFile()` → `file_get_contents($path)` → `HttpResponse::file()` の経路で、ファイルを丸ごとメモリに載せています。コード内のコメント自体にも「multi-gigabyte には向かない」と書かれています。PDF・画像の配信が中心なら別途 Apache の `X-Sendfile` や CDN が必要です。

**3. セッションストレージがデフォルト（ファイル）**

サーバが1台なら問題ないですが、ロードバランサ配下の複数台構成にすると即セッション不整合が起きます。Redis/Memcached への切り替えは自前実装になります。

**4. レートリミットなし**

ログインエンドポイントへのブルートフォースを Apache/nginx 側で対策しないと素通りします。

---

### 実務で困りやすいが対処可能

**5. 入力バリデーションライブラリなし**

現状はコントローラ内で `trim()`・`ctype_digit()` の手書きチェックのみ。案件の規模によっては、バリデーションコードが膨れ上がります。

**6. ページネーションヘルパーなし**

`DataMapperBase` に LIMIT/OFFSET の汎用メソッドがなく、各 Mapper で SQL を書くことになります。

**7. バックグラウンドジョブ機構なし**

メール送信・重いバッチ処理等の非同期処理は一切対象外。外部ツール（supervisord + 自作 CLI、Beanstalkd 等）を組み合わせる前提が必要です。

---

## 商用向けに補強するなら最低限これ

| 課題 | 対処方法 |
|---|---|
| マイグレーション | Phinx or Liquibase を外付け（フレームワーク外で管理） |
| セッション | php-redis + `session.save_handler=redis` の .env 設定 |
| ファイル配信 | Apache `X-Sendfile` or S3 presigned URL |
| レートリミット | Apache `mod_evasive` or nginx `limit_req` |

いずれも「NeNe の内部を書き換えなくても」対処できます。これはフレームワークが小さいことの利点です。

---

## 現実的な「適正規模」の目安

- **同時接続**: 〜50セッション程度なら何も考えなくて良い
- **データ量**: 数十万レコード程度は PDO + インデックスで問題なし
- **チーム規模**: 1〜3名が NeNe のコードを読める状態であること（フレームワーク自体が小さいので学習コストは低い）
- **PHP スキル**: フレームワークに頼らず PDO・セッション・HTTP をある程度理解しているメンバーがいること

---

## 一言で言うと

**「小〜中規模の社内業務システム、または既存レガシー PHP のリプレース先」としては商用実績が出せるレベル。外部公開・高可用・ファイル大量処理を同時に求める案件は、補強前提でないとリスクあり。**

マイグレーション機構の欠如だけは、案件受注前に「どう運用するか」の答えを用意しておくことを強く勧めます。
